
<%= simple_form_for @employee, :html => { :class => "form-horizontal" } do |f| %>
  <fieldset>

    <%= f.input :full_name %>
    <%= f.input :job_title %>
    <%= f.input :level %>
    <%= f.input :cost_center %>
    <%= f.input :contractor %>
    <%= f.input :part_time_status %>
    
    <%= f.input :subordinate_tokens, :label => "Reports", :input_html => { 'data-pre' => @employee.subordinates.map(&:attributes).to_json } %> 

    <div class="form-actions">
      <%= f.submit "Update Employee", :class => "btn btn-primary" %>
      <%= link_to "Cancel", employees_path, :class => "btn" %>
    </div>
  </fieldset>
<% end %>
