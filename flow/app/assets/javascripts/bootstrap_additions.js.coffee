# GF - OK - these we causing problems. not sure what commenting them out does, but now UJS with coffee works.

#jQuery ->
#  $("a[rel=popover]").popover()
#  $(".tooltip").tooltip()
#  $("a[rel=tooltip]").tooltip()
